# @react-three/fiber 9.x declares a peer range of `react >=19 <19.3`, while this
# app runs React 19.3. The two are compatible in practice (R3F only uses public
# React APIs), so resolve peers loosely instead of forcing every contributor to
# remember `--legacy-peer-deps` on a fresh clone.
legacy-peer-deps=true
